[package]
name = "linkscope-macros"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[lib]
proc-macro = true

[features]
runtime = []

[dependencies]
quote = "1"
syn = { version = "2", features = ["full"] }
